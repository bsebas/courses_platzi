// []

let users:string[];
users=['fas','gsgs','fsafas'];

// users=[1,true,'sa']

// Inferifo
let other=['fasfas','fsaf']

// Array <type>
let picture:Array<string>;
picture = ['favorite','fsdsa','fadas']

// Get values

console.log(users[0])
console.log(users.length)

users.forEach(e=>{
    console.log(e)
})